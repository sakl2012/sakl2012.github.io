<?xml version="1.0" encoding="utf-8"?>
<sited>
    <meta>
        <title>gimy 劇迷</title>
        <intro>2016-2026 Gimy 劇迷 - 高清追劇首選</intro>
        <author>sakl2012</author>
        <encode>utf-8</encode>
        <url>gimytw.cc</url>
        <updateUrl>https://sakl2012.github.io/sited/gimy.sited.xml</updateUrl>
    </meta>

    <main>
        <home url="https://gimytw.cc/" parse="home_list_parse" />
        <tags tagParse="list_parse">
            <item group="電視劇" title="全部" url="https://gimytw.cc/drama/"/>
            <item group="電影" title="全部" url="https://gimytw.cc/movie/"/>
            <item group="動漫" title="全部" url="https://gimytw.cc/anime/"/>
            <item group="綜藝" title="全部" url="https://gimytw.cc/variety/"/>
            <item group="最新" title="全部" url="https://gimytw.cc/latest/"/>
        </tags>
        <search parse="search_parse" url="https://gimytw.cc/search?q=@key&amp;page=@page"/>
        <book parse="book_parse"/>
        <section parse="section_parse"/>
    </main>

    <require>
        <item url="https://sakl2012.github.io/sited/cheerio.js" lib="cheerio" />
        <item url="https://sakl2012.github.io/sited/gbk2big5.js" lib="gbk2big5" />
    </require>

    <script>
        <![CDATA[
        const baseUrl = "https://gimytw.cc";

        function home_list_parse(url, html){
            const $ = cheerio.load(html);
            let list = [];
            $('div.layout-box').each(function() {
                const $$ = cheerio.load($(this).html());
                const group_name_raw = $$('div.box-title h3').text().trim();
                if (group_name_raw) {
                    const group_name = gbk2big5.traditionalized(group_name_raw);
                    $$('a.video-pic').each(function() {
                        const link = cheerio(this);
                        const videoUrl = link.attr('href');
                        const name = link.attr('title');
                        const logo = link.attr('data-original') || link.attr('data-src');
                        if (videoUrl && name) {
                            list.push({
                                group: group_name,
                                url: baseUrl + videoUrl,
                                name: gbk2big5.traditionalized(name),
                                logo: logo ? (logo.startsWith('http') ? logo : baseUrl + logo) : null
                            });
                        }
                    });
                }
            });
            return JSON.stringify(list);
        }

        function list_parse(url, html){
            const $ = cheerio.load(html);
            let list = [];
            $('a.video-pic').each(function() {
                const link = cheerio(this);
                const videoUrl = link.attr('href');
                const name = link.attr('title');
                const logo = link.attr('data-original') || link.attr('data-src');
                if (videoUrl && name) {
                    list.push({
                        url: baseUrl + videoUrl,
                        name: gbk2big5.traditionalized(name),
                        logo: logo ? (logo.startsWith('http') ? logo : baseUrl + logo) : null
                    });
                }
            });
            return JSON.stringify(list);
        }

        function search_parse(url, html){
            const $ = cheerio.load(html);
            let list = [];
            $('a.video-pic').each(function() {
                const link = cheerio(this);
                const videoUrl = link.attr('href');
                const name = link.attr('title');
                const logo = link.attr('data-original') || link.attr('data-src');
                if (videoUrl && name) {
                    list.push({
                        url: baseUrl + videoUrl,
                        name: gbk2big5.traditionalized(name),
                        logo: logo ? (logo.startsWith('http') ? logo : baseUrl + logo) : null
                    });
                }
            });
            return JSON.stringify(list);
        }

        function book_parse(url, html) {
            const $ = cheerio.load(html);
            let sections = [];
            $('div.playlist').each(function() {
                const $$ = cheerio.load($(this).html());
                const group = $$('.gico, .box-title').text().trim() || "集數列表";
                $$('ul li a').each(function() {
                    const episodeLink = cheerio(this);
                    const episodeUrl = episodeLink.attr('href');
                    if (episodeUrl) {
                        sections.push({
                            group: gbk2big5.traditionalized(group),
                            name: gbk2big5.traditionalized(episodeLink.text().trim()),
                            url: baseUrl + episodeUrl
                        });
                    }
                });
            });

            let logoUrl = $('a.video-pic').attr('data-original') || $('a.video-pic').attr('data-src');
            const intro = $('span.details-content-default, span.details-content-all, div.item-desc').text().trim();

            const book = {
                name: gbk2big5.traditionalized($('h1').first().text().trim()),
                logo: logoUrl ? (logoUrl.startsWith('http') ? logoUrl : baseUrl + logoUrl) : null,
                intro: gbk2big5.traditionalized(intro.replace('線上看:', '').trim()),
                sections: sections
            };

            return JSON.stringify(book);
        }

        function section_parse(url, html) {
            return JSON.stringify({
                type: "webview",
                data: url
            });
        }
        ]]>
    </script>
    <WebViewInjection>
        <![CDATA[
            javascript:(function() {
                function cleanupAndMaximizePlayer() {
                    try {
                        const player = document.querySelector('iframe[name="p-frame"], .player_left, #zanpiancms_player');
                        if (player) {
                            const playerParent = player.parentNode;
                            playerParent.removeChild(player);

                            document.body.innerHTML = '';
                            document.body.appendChild(player);

                            document.body.style.margin = '0';
                            document.body.style.padding = '0';
                            document.body.style.overflow = 'hidden';
                            player.style.position = 'absolute';
                            player.style.top = '0';
                            player.style.left = '0';
                            player.style.width = '100vw';
                            player.style.height = '100vh';

                            return true;
                        }
                        return false;
                    } catch (e) {
                        return false;
                    }
                }

                let attempts = 0;
                const maxAttempts = 20;
                const intervalId = setInterval(function() {
                    attempts++;
                    if (cleanupAndMaximizePlayer() || attempts >= maxAttempts) {
                        clearInterval(intervalId);
                    }
                }, 500);
            })();
        ]]>
    </WebViewInjection>
</sited>